# 智谱 AI API Key — 从 https://open.bigmodel.cn/ 控制台获取
# 复制为 .env.local 后填入，切勿提交到版本库
#
# 注意：变量名**没有** VITE_ 前缀，保证只在 Node 端 (vite.config.ts 反向代理) 使用，
# 绝不会被 Vite 注入到浏览器 bundle 里。
ZHIPU_API_KEY=your_zhipu_api_key_here
